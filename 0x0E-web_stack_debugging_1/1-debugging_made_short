#!/usr/bin/env bash
# this solution for task 1 worked except for 1 check
ln -sf /etc/nginx/sites-available/* /etc/nginx/sites-enabled/
echo 'leh keda bas ' > /var/run/nginx.pid  /run/nginx.pid
sudo service nginx restart
