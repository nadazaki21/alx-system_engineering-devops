#!/usr/bin/env bash
# scipt to make fix nginx and make it work on port 80
sudo apt update -y
sudo apt install nginx --fix-missing -y
sudo sed -i "s/8080/80/g" /etc/nginx/sites-enabled/default
sudo service nginx restart
